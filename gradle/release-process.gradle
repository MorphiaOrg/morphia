apply plugin: 'release'

release {
    // the version in the build file should be a SNAPSHOT version
    String version = project('morphia').version
    releaseVersion = version[0..<version.indexOf('-SNAPSHOT')]
}

apply from: 'gradle/release-to-nexus.gradle'