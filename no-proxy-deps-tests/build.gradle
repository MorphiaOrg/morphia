sourceSets {
    main {
        java.outputDir = project.project(":util").sourceSets.test.output.files[0]
    }
}
dependencies {
    compile(project(':morphia')) {
        exclude group: 'cglib', module: 'cglib-nodep'
    }
}
