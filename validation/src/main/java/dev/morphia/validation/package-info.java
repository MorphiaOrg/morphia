/**
 * Provides integration support for Bean Validation (JSR-380) within Morphia,
 * enabling constraint validation on entity fields during persistence operations.
 */
package dev.morphia.validation;
