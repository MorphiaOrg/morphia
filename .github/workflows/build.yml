name: Build Project

on:
  push:
  workflow_dispatch:

jobs:
  Build:
    strategy:
      matrix:
        java: [ 11, 17, 19 ]
        mongo: [ 6.0.2, 5.0.13, 4.4.17, 4.2.23 ]
    uses: evanchooly/workflows/.github/workflows/build.yml@master
    with:
      java: ${{ matrix.java }}
      archive-name: "${{matrix.mongo}}-${{matrix.java}}"
      maven-flags: "-DMONGODB=${{ matrix.mongo }}"

  Quality:
    name: Style Checks
    uses: evanchooly/workflows/.github/workflows/build.yml@master
    with:
      maven-flags: "-Pquality -DskipTests"

  Snapshots:
    if: github.ref_type == 'branch'
    needs:
      - Build
      - Quality
    uses: evanchooly/workflows/.github/workflows/release.yml@master
    secrets:
      SONATYPE_USERNAME: ${{ secrets.SONATYPE_USERNAME }}
      SONATYPE_PASSWORD: ${{ secrets.SONATYPE_PASSWORD }}
      MAVEN_GPG_PASSPHRASE: ${{ secrets.MAVEN_GPG_PASSPHRASE }}
      GPG_PRIVATE_KEY: ${{ secrets.GPG_PRIVATE_KEY }}
      GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}

  Docs:
    name: Update Documentation
    runs-on: ubuntu-latest
    needs:
      - Snapshots
    steps:
      - name: Invoke antora build
        uses: benc-uk/workflow-dispatch@v1
        with:
          workflow: Docs
          repo: MorphiaOrg/morphia-docs
          token: ${{ secrets.PUSH_TOKEN }}
          ref: refs/heads/master
