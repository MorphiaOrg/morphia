zipConversionStage = {
   $addFields: {
      convertedZipCode: { $toString: "$zipcode" }
   }
};

sortStage = {
   $sort: { "convertedZipCode": 1 }
};
db.orders.aggregate( [
  zipConversionStage,
  sortStage
] 