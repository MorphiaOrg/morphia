qtyConversionStage = { 
   $addFields: { 
      convertedQty: { $toLong: "$qty" }
   }
};

sortStage = {
   $sort: { "convertedQty": -1 }
};
db.orders.aggregate( [
   qtyConversionStage,
   sortStage
]