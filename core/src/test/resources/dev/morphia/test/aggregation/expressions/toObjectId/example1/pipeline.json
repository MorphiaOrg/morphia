idConversionStage = { 
   $addFields: { 
      convertedId: { $toObjectId: "$_id" }
   }
};

sortStage = {
   $sort: { "convertedId": -1 }
};
db.orders.aggregate( [
   idConversionStage,
   sortStage
] 